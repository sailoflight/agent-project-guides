# 新项目与持续开发 Agent 指南

> 适用角色：初始化新项目的开发 agent，以及项目完成治理后的日常开发 agent。
>
> 路径约定：本文中的 `profiles/...`、`templates/...` 等包内路径，以当前治理包目录为基准；`AGENTS.md`、`docs/...` 等交付路径，以目标项目根目录为基准。
>
> 新项目初始化时执行第 0-8 节；日常开发主要执行第 9-13 节。不要预读已有项目的维护迁移指南。

## 0. 加载机制和接入方式

兼容 harness 会在首轮前加载项目根到当前工作目录路径上的精确指令文件，例如 `AGENTS.md` 或 `CLAUDE.md`，并在后续 model step 重新探测 baseline。根 README 和任意嵌套治理目录不会因为存在或被链接而自动加载；新出现的后代目录规则仍需文件工具触碰对应路径后注入。

接入方式二选一：

- 人工方式不注入任何治理提示词。负责人或明确承担初始化任务的 agent 主动读取本指南，按模板直接创建或合并最终项目根 `AGENTS.md` 和必要文档；完成后日常 agent 只看到项目专属规则。
- 自动 handoff 从包内运行 `scripts/install.sh handoff`，将已有根入口原样保存为 `AGENTS_origin.md`，把原内容镜像进临时入口保持约束生效，再安装 `bootstrap/AGENTS.handoff.md` 的一次性接管指令。

若本次 session 由临时 handoff 启动，必须先执行其中的合并门：读取 `AGENTS_origin.md`（若存在）、本指南第 0-8 节、匹配 profile 和必要模板小节；生成最终项目入口；确认原约束已保留后删除 handoff/origin-mirror 标记，但保留未修改的精确备份供人工复核；重新读取最终 `AGENTS.md` 后才开始产品修改。不得把临时接管文件当作最终项目规范。

标准 DSH code preset 当前为自动指令设置 65,536 bytes 总预算，但包内 handoff 安装器把临时根入口限制为 16,384 bytes，并在发现其他根候选指令时拒绝接管。根入口仍建议控制在约 2K tokens 内；预算余量应留给目录级局部规则和其他 authority instructions，不能用上限证明塞入长文合理。

## 1. 初始化目标

项目开始时就建立最小、可维护的开发契约，避免代码进入中型规模后才依靠 agents 逐目录猜测。

初始化不要求一次写完所有文档。完成后应满足：

- 开发 agent 有精简且确定的根入口；
- 项目当前架构、主要模块和依赖方向有记录；
- 修改类型能够映射到真实验证命令；
- 开发者、消费者、运维和最终用户的说明按角色分离；
- 配置、状态、数据、生成物和秘密有明确归属；
- 公共或高风险模块有契约；
- 项目后续变化有明确的文档触发规则。

## 2. 先确定项目 Profile

根据已经确定的产品形态选择一个或多个匹配 profile：

- MCP server：`profiles/MCP_PROJECT.md`
- 纯函数库、SDK 或 CLI：`profiles/LIBRARY_AND_CLI_PROJECT.md`
- 应用、后端服务、GUI、monorepo 或数据项目：`profiles/APPLICATION_SERVICE_MONOREPO.md`

只打开匹配文件。项目形态尚未确定时，不要为了文档模板提前决定架构；先记录为 `unknown` 并完成最小通用入口。

## 3. 初始化任务卡

```text
项目目标：要向谁提供什么能力
项目类型：library / CLI / MCP / service / GUI / monorepo / data / other
实际角色：developer / consumer / operator / end user
入口：主要运行、导出、注册或构建入口
模块：首批职责边界
风险：网络、生产写入、秘密、配额、迁移、破坏性操作
验证：单元、集成、构建、静态、视觉或人工检查
交付：本轮实现和文档的完成条件
未知：尚未决定且不应被猜测的架构问题
```

用户没有提供的字段优先从已经存在的代码和配置补全。会改变产品行为、安全边界、兼容性或成本的未决项才需要询问。

## 4. 创建最小项目内入口

如果项目根当前是 handoff 临时入口，以它为迁移载体，不要另建第二个根入口。根据项目规模创建或合并以下能力：

```text
AGENTS.md                         harness 自动加载的开发 agent 薄入口
docs/INDEX.md                    角色/任务路由
docs/architecture/OVERVIEW.md    当前架构和模块边界
docs/verification/MATRIX.md      修改类型到验证方式
```

实际写入时，按需读取 `templates/CORE_DOCUMENT_TEMPLATES.md` 的精确小节。根 `AGENTS.md` 必须包含实际生效的硬约束和最小路由，不能只写“请先读 README”。

很小的纯函数库可以把 INDEX、架构和验证矩阵合并进一份开发文档，但必须明确回答这些契约问题。不要为不适用的角色创建空目录。handoff 的最终入口生成后删除 `handoff` 和 `origin-mirror` 临时标记；`AGENTS_origin.md` 必须保持未修改并报告供人工复核。人工方式直接交付不含任何治理 bootstrap 的最终入口。

## 5. 根 `AGENTS.md` 规则

根入口只放：

- 项目一句话定位；
- 当前开发 agent 的最小查找顺序；
- 跨模块安全和变更红线；
- 文档索引、架构和验证矩阵入口；
- 保护并行修改、生成物和秘密的规则。

不要放：

- 完整 API/tool/CLI 目录；
- 生产消费者教程；
- 完整部署手册；
- 各模块实现细节；
- 已完成历史和未来 roadmap；
- 可以从代码或 schema 生成的动态事实。

建议控制在约 2K tokens 内。目录级 `AGENTS.md` 只添加局部覆盖，不复制根规则。兼容 harness 会在 agent 读、写或编辑该目录下文件时注入路径上的局部入口，因此真正必须在进入模块前生效的局部红线应放在最近的目录级 `AGENTS.md`，而不是只藏在 README 或远端索引中。

## 6. 建立模块契约

项目开始时只为以下模块建立契约：

- 首批公共接口或工具注册模块；
- 会执行网络、生产写入、迁移或破坏性操作的模块；
- 配置、状态、凭据或持久数据所有者；
- 跨平台或外部系统边界；
- 依赖方向不显然的核心模块。

模块契约至少包括：

```text
Owns
Does not own
Entrypoints
Contracts and invariants
Allowed dependencies
Data/config/generated ownership
Verification
Documentation triggers
```

新增目录或文件不自动要求新增文档。只有形成新的职责边界、公共契约或特殊风险时才新增模块说明。

模块契约可以拆成两个投递面：

- `docs/modules/<module>.md` 保存完整职责、证据、接口边界和验证映射；
- `<module>/AGENTS.md` 仅在进入该目录前必须机械生效时创建，保存短小的局部红线、禁止依赖、数据所有权和验证入口，并链接权威模块契约。

两处不得复制同一长事实。目录级入口只保留必须立即执行的摘要，动态清单和详细解释仍以代码、schema 或模块契约为权威来源。

## 7. 按投递渠道分离角色

- 开发 agent：项目 `AGENTS.md`、development、architecture、modules、verification。
- 库/API/CLI 消费者：usage、生成 API/command reference、示例。
- MCP 消费 agent：MCP instructions、tool schema、resources/prompts；不能依赖仓库开发入口。
- 运维：operations、部署、配置、监控、回滚和事故恢复。
- 最终用户：README 和产品使用文档。

同一安全事实可以服务多个角色，但应有一个结构化或明确的权威来源，再为各投递渠道生成或摘要，不能复制多份易漂移长文。

## 8. 初始化验收门

开始大规模功能开发前验证：

- 根入口能将开发任务路由到相关模块；
- 当前架构只描述已成立内容，未决定项标为 `unknown`；
- 主要入口和验证命令真实存在；
- 公共和高风险模块有职责与非职责；
- 配置、状态、生成物和秘密归属明确；
- 消费者或运维角色只在实际存在时建立对应入口；
- 没有手工复制可生成的工具、接口或参数清单；
- handoff 的根 `AGENTS.md` 不再包含 `handoff` 或 `origin-mirror` 标记，且 `AGENTS_origin.md` 保持未修改并已报告供人工复核；人工入口不存在治理包跳转或临时提示词；
- 一个无历史上下文的 agent 能在没有额外“先读文档”提示词的情况下定位首个功能及其测试。

初始化产物应与首批代码一起演进，不应先写一套脱离实现的完整未来架构。

## 9. 日常任务归一化

简单、明确、机械的修改可以直接执行。非简单任务先形成短任务卡：

```text
目标：用户可观察结果
范围：允许修改的模块和接口
非目标：明确不处理的内容
验收：行为、测试或输出
约束：兼容、安全、性能、网络和外部成本
证据：模块契约、实现入口及测试
验证：必须运行的命令或人工检查
权限：生产、迁移、秘密和破坏性操作
未决：会改变产品行为或任务规模的选择
```

歧义处理：

1. 仓库证据可确定 -> 查询并记录依据，不询问。
2. 内部实现有多种选择 -> 遵循现有架构和最小变更原则。
3. 会影响行为、数据、安全、兼容或外部成本 -> 集中澄清一次。

## 10. 日常检索和上下文预算

默认顺序：

```text
分类任务
  -> 根 AGENTS.md
  -> docs/INDEX.md 或匹配索引
  -> 一个模块契约
  -> 命中的实现和测试
  -> 必要时扩展到架构、ADR、knowledge 或 evidence
```

首次读取通常限于一个根入口、一个路由、一个模块契约、命中的代码及测试。搜索先返回有限候选；大型索引、日志、生成物、完整 schema 和原始参考不是首次读取对象。

上下文预算不是不可突破的硬限制。局部证据不足时应明确原因后扩展读取，正确性优先。

## 11. 实现和验证纪律

- 保持变更在目标模块和公共契约范围内。
- 尊重现有架构和项目惯例，不为小改动引入新抽象。
- 保留用户或其他 agents 的并行修改。
- 不手工编辑生成物，除非项目契约明确要求。
- 生产、真实网络、迁移、破坏性操作和秘密访问必须服从项目权限门。
- 根据 `docs/verification/MATRIX.md` 选择验证，不依赖记忆猜命令。
- 风险越高、跨模块越多，验证范围越大。

## 12. 变更后的文档判断

| 变化 | 动作 |
|---|---|
| 公共 API、tool、命令参数或输出 | 更新消费者契约或重新生成参考 |
| 模块职责或依赖方向 | 更新 architecture/module，必要时 ADR |
| 配置、部署、监控或恢复 | 更新 operations |
| 权限、安全、网络成本或副作用 | 更新权威风险信息及相关角色视图 |
| Bug 揭示可复用且已验证认知 | 更新 knowledge，并链接 evidence |
| 仅内部实现且契约不变 | 通常无需改文档，只需验证 |
| 尚未实现的设想 | 只进入 roadmap |

不要机械执行“每次代码变化都改文档”；只同步发生变化的契约。

## 13. 日常完成定义

非简单任务结束前确认：

- 用户可观察验收已经满足；
- 匹配的测试、构建、静态或人工验证已经执行；
- 公共契约和模块边界没有被静默改变；
- 必要的生成物已经由权威来源重建；
- 文档触发矩阵已经检查；
- 未验证内容没有被写成事实；
- 无法执行的验证和剩余风险已明确报告。

## 14. 禁止事项

- 不得为一个任务先读完整仓库或完整文档树。
- 不得把生产使用指南塞入开发 agent 自动入口。
- 不得把 roadmap 当成当前能力。
- 不得为每个文件创建说明文档。
- 不得手工复制动态工具、参数和 API 列表。
- 不得为了降低 token 而跳过必要证据和验证。
- 不得在项目完成初始化后让日常开发 agent 每次重新读取本治理文档包。
